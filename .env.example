# Genius365 Design Factory - environment template.
# Copy to ~/.hermes/.env and paste the value. NEVER commit a real key.
# If you already use Hermes with OpenAI, this is already set - nothing to do.

# Required - your OpenAI API key (the provider your team already uses; also powers GPT Image 2).
OPENAI_API_KEY=
